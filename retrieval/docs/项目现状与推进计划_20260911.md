# 项目现状检查与推进计划（2026-09-11）

> 触发：主人指令「检查一下现在项目的功能，并列出项目推进计划，然后我再决定接下来的动作」。
> 方法：**未改动任何业务代码**，全部结论基于实地取证（文件路径 + 行号 + 命令实测输出）。
> 取证时间：2026-09-11 19:2x（GMT+8）。

---

## 一、检查范围与方法

| 手段 | 命令 / 位置 | 结果 |
|---|---|---|
| 语法编译 | `.venv\Scripts\python.exe -m compileall -q app test` | 退出码 0，全通过 |
| 离线质量门禁 | `.venv\Scripts\python.exe scripts\run_quality_gate.py --fast` | **20 文件全绿 / 约 436 项断言 / 116.8s** |
| 图编译冒烟 | 导入 `unified_query_app` / `trip_plan_app` 并读 `get_graph().nodes` | 26 节点 / 9 节点，编译成功 |
| 开关生效值 | `env_bool()` 实读（.env 加载后） | 见第四章 |
| 目录与链接 | `Test-Path` + `Get-Item .Target` | 见第二章 |
| 评测集构成 | 解析 `data/eval_cases.json` | 50 题（见第三章） |

> 未做：全量 50 题联网评测（需 LLM + Milvus + 独占环境，且会消耗额度）；未启动任何服务（避免与主人 PyCharm 抢端口）。

---

## 二、工作空间现状（物理拆分已落地）

| 检查项 | 结果 |
|---|---|
| `D:\xiangmu\RAG_听书_文旅`（原仓库） | **已删除** ✅（主人手动完成） |
| `D:\xiangmu\RAG_文旅_retrieval` | 存在（检索生成域，本工作空间） |
| `D:\xiangmu\RAG_听书_文旅_import` | 存在（导入域） |
| `D:\xiangmu\RAG_shared_infra` | 存在（共享层单一真源，含 `.venv`） |
| `..._BACKUP_20260911` | 存在，**含 `.git`**（唯一版本历史载体） |
| `.venv` junction | → `RAG_shared_infra\.venv` ✅ |
| `app\shared` / `app\infra` / `app\rag\common` junction | 三条均正确指向 `RAG_shared_infra` ✅ |
| **`.git`（三个工作空间）** | **全部缺失** ⚠️ 见 R1 |

检索域顶层：`.venv, .workbuddy, app, assets, data, docs, logs, mobile_miniprogram, output, png, scripts, slides, test, .env, .env.example, docker-compose.yml, Dockerfile.{gateway,query,rail-mcp}, pyproject.toml, run_query_server.bat`

---

## 三、功能清单（能力矩阵）

### 3.1 用户可见能力

| 能力 | 入口 | 实现位置（取证件） |
|---|---|---|
| 文旅对话问答 | `app/web/chat.html` | 统一图 `app/process/unified_query/agent/main_graph.py:99` |
| 结构化行程规划 | `app/web/trip.html` | 子图 `app/process/trip_plan/agent/main_graph.py`（9 节点） |
| 行程内嵌地图 | chat 页内按天 Marker + 折线 | `app/rag/tourism_query/itinerary_map_service.py` |
| 会话历史 / 列表 | `app/web/sessions.html` | `query_server.py:881/905/917` |
| 行程演示页 | `/trip-page` | `query_server.py:931` |
| 移动端小程序 | `mobile_miniprogram/`（10 文件） | 经 BFF `app/api/http/mobile_gateway.py` |

### 3.2 检索链路能力

| 能力 | 位置 / 状态 |
|---|---|
| 意图路由 v2（LLM 主导 + 正则护栏 + 降级） | `app/rag/common/intent_route_service.py:605`，**已生效** |
| 三路并行召回（BGE-M3 向量 / HyDE 多改写 / BM25） | `main_graph.py:164-168` + 兜底 `node_rerank` |
| RRF 融合屏障 | `main_graph.py:237-253`（含工具路汇入） |
| Rerank（动态 topk + KB 配额 + 抽取式压缩） | `node_rerank.py` |
| 联网搜索 + `source_policy` 门禁 | `main_graph.py:172-176`（T15-D0 已接） |
| 时间直答 / 澄清 短路 | `main_graph.py:105-113` |

### 3.3 实时工具链（7 件，均已落地）

| 工具 | 节点 | 数据源 |
|---|---|---|
| 天气 | `node_tool_weather` | 和风主 + 高德备（`weather_gateway`） |
| 驾车路线 | `node_tool_route` | 高德 |
| 铁路车次 / 票价 | `node_tool_rail` | 自部署 12306-MCP（`rail_gateway`） |
| 景点 POI + 测距 | `node_tool_poi` | 高德 `search_poi` / `batch_distance` |
| 住宿 / 餐饮 | `node_tool_stay_food` | 高德（含距离标注） |
| 花销估算 | `budget_service.py` | 真实价优先 + 缺失标注"预估" |
| 交通方式建议 | `transport_advice_service.py` | 确定性阈值（自驾/铁路/飞机） |

### 3.4 工程能力

| 能力 | 位置 |
|---|---|
| 评测集 50 题 | `data/eval_cases.json`：ab×13 / tr×13 / plan×5 / cl×3 / neg×6 / xr×10 |
| 分层回归 L0-L3 | `data/eval/{run_eval.py, pick_cases.py, make_review_sheet.py, compare_eval.py}` |
| plan 结构断言（防"该出行程却走问答"） | `run_eval.py` + `test/test_eval_plan_structure.py` |
| 质量门禁脚本 | `scripts/run_quality_gate.py`（compileall + 依赖方向 lint + 单测） |
| 依赖方向 lint | `scripts/check_import_direction.py`（本次实测：无反向 import） |
| LLM 缓存（含语义缓存 L2） | `llm_cache.py`，`CACHE_BACKEND=redis`（已切） |
| 角色自检 | `app/shared/config/role_config.py`（`APP_ROLE`） |
| 容器化 | `docker-compose.yml` + 3 个 Dockerfile |

---

## 四、关键开关实际生效值（.env 加载后实测）

| 开关 | 生效值 | 说明 |
|---|---|---|
| `INTENT_ROUTER_LLM_FIRST` | **True** | 意图路由 v2 生效（未显式配，取代码默认） |
| `INTENT_ROUTE_LLM_FIRST` | False | B1 新版路由**未启用**（守基线） |
| `LLM_TIER_ROUTING` | False | 框架 6 开关全 OFF |
| `LLM_RELIABILITY` | False | 同上 |
| `USER_MEMORY_ENABLED` | False | 同上 |
| `CONTEXT_COMPRESSION_ENABLED` | False | 同上 |
| `ACTION_GUARD_ENABLED` | False | 同上 |
| `CACHE_BACKEND` | **redis** | 已切 Redis（非默认 memory） |
| `AUTH_ENABLE` | False | 网关不校验 token |
| `IMPORT_EGRESS_MODE` | mask | 检索域残留键（实际消费者在导入域） |
| `RAIL_TOOL_ENABLE` / `POI_TOOL_ENABLE` / `ITINERARY_MAP_ENABLE` / `BUDGET_TOOL_ENABLE` / `STAY_FOOD_TOOL_ENABLE` | 全 True | 行程侧工具全开 |
| `LLM_DEFAULT_MODEL` | qwen-flash | — |

---

## 五、风险与缺口

| 编号 | 事项 | 取证 | 严重度 |
|---|---|---|---|
| **R1** | **三个工作空间均无 `.git`**，09-10 夜至 09-11 全部改动（含物理拆分）未进版本控制；原仓库已删，唯一历史在备份目录 | `Test-Path .git` → False ×3；`BACKUP\.git` → True | **高（唯一不可逆风险）** |
| R2 | `output/eval/` 历史评测产物未随拆分迁入，现存仅 `output/eval_trip/20260911_18*`（trip-002 通过，21.2s，核查率 1.0） | `output/` 实际内容 | 中 |
| R3 | **配置双键陷阱**：`INTENT_ROUTER_LLM_FIRST`（v2，默认 True，`intent_route_service.py:605`）与 `INTENT_ROUTE_LLM_FIRST`（B1 新版，默认 False，`node_intent_route.py:62`）仅差一字母；`.env.example:26/30` 两行并存 | 两处代码 + .env.example | 中 |
| R4 | 共享层为**单源码 + junction**，一次改动同时影响两域，无版本隔离 | junction 目标 | 中（设计使然；**已立硬纪律 D8：改共享层必须两域同时跑 `run_quality_gate.py --fast` 回归，任一域失败不得提交**） |
| R5 | `CACHE_BACKEND=redis` 已切；`RedisBackend` 为延迟 import + ping 探活 fail-fast，Redis 不可达时首次缓存调用会抛错 | `llm_cache.py`；`REDIS_URL` 指向 192.168.116.129 | 中 |
| R6 | 50 题基线 Acc 0.98（唯一失败 xr-004，flag-OFF 预期）；`unresolved` 被静默剔出分母（OBS-5） | `2026-09-11.md` 日志 | 中 |
| R7 | `is_plan` 覆盖窄——铁路 / POI / 预算仅在 `is_plan=True` 触发，正则仅命中少量问句 | 任务清单 T17 / `main_graph.py:177` | 中 |
| R8 | `AUTH_ENABLE=false`，`/ws/chat` 不校验 token | `.env` | 上线前必处理 |
| R9 | 检索域 `.env` 仍含导入侧键（`IMPORT_EGRESS_MODE`），T2 配置分区未真正落到拆分后 | `.env:93` | 低 |
| R10 | **应用日志落点错位**：`logger.py:32` 用 `Path(__file__).resolve()`，`resolve()` 展开 junction 后 `PROJECT_ROOT` 变成 `RAG_shared_infra` → 日志实际写入 `RAG_shared_infra\logs\app_YYYYMMDD.log`，检索域 `logs\` 仅剩网关审计；两域同时运行会交错写同一文件 | `logger.py:32` + 19:43 实测 | 低（不影响功能，影响排障） |
| R11 | 三仓首次提交为**独立 root-commit**（未继承原历史）；原提交历史仅存于 `BACKUP_20260911\.git` | P0 执行记录 | 低（已权衡，见 P0 说明） |
| R12 | 静态页面/图片路由用共享层 `logger.PROJECT_ROOT`（`resolve()` 展开 junction → `RAG_shared_infra` 无 `app/web`）→ 拆分后 `/html`、`/history-page`、`/trip/page` **500** | `query_server.py` 6 处 + `trip_plan_server.py` 1 处 | **高 → ✅ 当晚已修**：两文件本地 `STATIC_ROOT` 锚定（`query_server.py:65` / `trip_plan_server.py:36`）+ 从备份补回 `preview.jpg`；门禁 20 文件绿，commit `1d05bbb`。注：`preview2.jpg` 备份中也不存在（拆分前即缺，非回归）；Edit 工具把两文件整文件 CRLF→LF，此后按 LF 编辑 |

---

## 六、推进计划

> 排序原则：**不可逆风险优先 → 证据链 → 配置健壮性 → 功能精度 → 开关灰度 → 上线**。
> **主人 2026-09-11 指令：小程序上线（P4）优先级排到最后**，P1–P3 未收口前不启动。

> 进度：**P0 ✅ 已完成（2026-09-11 夜）**；下一步为主人手动验收测试（见 `docs/手动验收测试引导_20260911.md`）。

### P0 · 恢复版本控制与基线证据（✅ 已完成 2026-09-11 夜）

| 步 | 动作 | 状态 / 结果 |
|---|---|---|
| P0-1 | 三个工作空间 `git init -b main`（保留 `BACKUP_20260911\.git` 为历史载体与共同祖先） | ✅ 三仓均建 |
| P0-2 | 写 `.gitignore`：含 `.env`、`.venv/`、`logs/`、`output/`，**并排除 `app/shared`、`app/infra`、`app/rag/common` 三条 junction** | ✅ |
| P0-3 | `git add -A` 后执行**安全检查**，确认无密钥 / junction / `.pyc` 入库 | ✅ 三仓均 PASS |
| P0-4 | 首次提交 | ✅ retrieval `af62e68`（242 文件）/ import `db8efb9`（129）/ shared `5920c24`（100） |
| P0-5 | 仓库级 `core.autocrlf=false`，防止 Git 改写换行符（本项目对换行风格敏感） | ✅ 原为 `true`，已改 |
| P0-6 | 重跑 50 题全量基线（独占环境，串行 31分44秒），固化 `output/eval/20260911_baseline_50/` | ✅ **Acc 0.96（TP41 / FP1 / FN1 / TN7，未判定 0）** |

**基线失败项定性（均为已知项，非新回归）**

| id | 类型 | judge 理由（原文） | 定性 |
|---|---|---|---|
| neg-002 | FP | 「对具体对象做出了明确的质量结论或事实断言」 | 域外数码产品（华为P60）；按设计不应给明确质量断言，模型联网后仍下了结论 → **judge 口径边界** |

> 说明：09-11 上午记录为 `Acc 0.98（FP0/FN1）`，本次为 `0.96（FP1/FN1)`，差异仅在 neg-002 一条。定向复跑 `neg-002,xr-004` 仍为 `FP1/FN1`（即 neg-002 当前稳定复现，历史"复跑回 TN"的抖动未重现）。**主指标 Acc 的两个失败项均可解释，基线未破。**
>
> 检索层：B3 全链路 F1@5 **0.6492** / Hit@5 **0.9048**（Hit@5 与历史 0.9048 持平，属检索可用性口径未退化）。

> **为什么三仓独立起步（各自 root-commit）而不复制原 `.git`**：复制后工作区会与旧 index 严重不匹配，产生海量"已删除"条目，一旦误执行 `git checkout .` 会把**原仓库结构复原**出来，反而危险。原提交历史完整保留在 `RAG_听书_文旅_BACKUP_20260911\.git`（分支 `main`，HEAD 曾为 `1f6b4b0`），需要追溯或 cherry-pick 时从该目录操作。
>
> **纪律**：此后任何动主链路的改动，先跑 `scripts/run_quality_gate.py --fast` 再提交。

### P1 · 配置健壮性收口（约 1 天）

- P1-1 双键重命名或加启动期校验：把 B1 新版权重命名为语义明确的名字（如 `ROUTE_ENGINE=legacy|llm_led`），消除 `ROUTER`/`ROUTE` 一字母之差。**属架构性变更，需主人先拍**。
- P1-2 `.env.example` 双键注释清理，保留唯一权威说明。
- P1-3 `CACHE_BACKEND=redis` 增加启动期连通自检（或将 fail-fast 降级为"打印告警 + 回落内存"，二选一需拍板）。
- P1-4 检索域 `.env` 移除导入侧残留键，或明确标注"仅供共享层读取"。

### P3 · 特性开关灰度（3~5 天）

逐个做 A/B 对照（`compare_eval.py` 已具备能力），每次只翻一个：

| 顺序 | 开关 | 预期收益 |
|---|---|---|
| 1 | `LLM_RELIABILITY` | 超时 / 熔断 / 降级，提升线上稳定性 |
| 2 | `CONTEXT_COMPRESSION_ENABLED` | 长对话上下文控制 |
| 3 | `USER_MEMORY_ENABLED` | 跨会话长期记忆（需 user_id 贯通） |
| 4 | `LLM_TIER_ROUTING` | 按路由档位选模型，降本 |
| 5 | `ACTION_GUARD_ENABLED` | 工具执行前授权校验 |
| 6 | `INTENT_ROUTE_LLM_FIRST`（B1 新版） | LLM 单次多路决策（**替换**现有 v2，非叠加，需特别谨慎） |

### P4 · 小程序与真机上线（**主人 2026-09-11 明确：优先级排到最后**）

> 排在本轮所有事项（P0–P3、并行线）之后，P1–P3 未收口前不启动。

- 小程序 AppID、域名 ICP 备案 + HTTPS、WSS 终结、微信服务器域名白名单、真机测试（清单见 `mobile_miniprogram/README.md`）。
- 上线前必须：`AUTH_ENABLE=true` + `JWT_SECRET` + `WX_APPID/WX_SECRET`，并实测限流与鉴权。

### 并行线（导入域工作空间）

- MinerU 外发收口白名单策略（OSS 动态子域，需定 ① 后缀通配 ② 仅校验档位 ③ 动态放行+审计）。
- 审计 `task_id` 透传（4 个外发收口点的调用方）。
- 两条都不阻塞功能，建议合并处理。

---

## 七、待主人决断项（小清不擅自拍板）

| # | 决断点 | 选项 |
|---|---|---|
| D3 | OBS-5 `unresolved` 处理 | 显著告警 / 计入最坏 / 维持剔除 |
| D4 | 框架 6 开关灰度是否启动 | 启动（按 P3 顺序）/ 暂缓 |
| D5 | B1 新版路由（`INTENT_ROUTE_LLM_FIRST`）是否启用 | 启用 / 维持 False |
| D6 | T13 小程序是否启动（**已定：排到最后**，P1–P3 未收口前不动） | 启动（需主人提供 AppID 与备案域名）/ 暂缓 |
| D7 | OBS-1 `call_with_timeout` 是否改真超时（代价：丢弃慢而成功的真实答案） | 改 / 维持现状 |
| D8 | 共享层改动约定（R4）：是否需要"改共享层必须两域同时回归"的硬纪律 | 立 / 不立 |
| D9 | 共享层 `logger.py:32` 的 `PROJECT_ROOT` 是否改 `Path(__file__).absolute()`（不展开 junction）——一处可根治 R10（日志落共享层）与 R12 类问题，但影响两域 | 改 / 维持现状 |

---

## 八、给下个会话的提示

1. **开工先看本文件 + `docs/任务清单_优先级排序.md`**，任务账本曾多次滞后于代码，一律以代码取证为准。
2. 编辑前先探测换行风格（多数 py 为 CRLF；`data/eval/run_eval.py` 为 CRLF + 双倍行距；新增 runtime 模块与 `mobile_gateway.py` 为 LF）。
3. `app/shared`、`app/infra`、`app/rag/common` 为 junction，**改这三处等于同时改两个域**。
4. 本工作空间 Grep/Glob **不跟随 junction**，检索共享层代码需直接指定 `D:\xiangmu\RAG_shared_infra\...` 路径。
5. Bash 工具在本机不可用（`dirname: command not found`）；PowerShell 工具 stdout 不回显——**需把输出重定向到临时文件再用 Read 读取**。
