历史会话：
{history_text}

当前日期：{current_date}

当前用户问题：{query}

任务：为"对话内行程规划"抽取结构化参数。请从【当前问题】与【历史会话】中提取信息，输出 JSON。

字段定义：
1. city：目的地城市（如"杭州""北京""九寨沟"）。完全无法确定时留空字符串。
2. origin：出发地城市（如"成都"）。未提及留空字符串。
3. days：游玩天数，整数（如"3 天""两天一夜"→ 2、"玩 5 天"→ 5）。未提及留 0。
4. start_date：出发日期，格式 YYYY-MM-DD（如"9月20号出发"结合当前日期换算）。未提及留空字符串。
5. preferences：旅行偏好标签数组（如 ["自然风光", "美食", "历史文化"]）。未提及留空数组。
6. transportation：交通方式（如"公共交通""自驾""高铁"）。未提及留空字符串。
7. accommodation：住宿偏好（如"经济型酒店""民宿""亲子酒店"）。未提及留空字符串。
8. free_text：其它补充要求（节奏、同行人、特殊需求等）。
9. budget：总预算金额（整数元，如"预算5000""3000块搞定"→ 5000）。未提及留 0。

规则：
- 历史会话中的信息仅在本轮明显在延续同一次规划时采信；若当前问题已开启一个全新的目的地 / 新行程，则历史里的旧信息不得算入本次。
- 不要编造用户未提及的内容；无法确定的字段一律留空（不要用常识默认、不要脑补）。
- days 只填数字本身，不要写单位。

请直接返回 JSON，不要输出任何解释文字：
{{
  "city": "",
  "origin": "",
  "days": 0,
  "start_date": "",
  "preferences": [],
  "transportation": "",
  "accommodation": "",
  "free_text": "",
  "budget": 0
}}
