历史会话：
{history_text}

当前用户问题：{query}

请根据历史会话和当前问题，提取用户正在询问的主体名称（attractions，可为景点名或文化主题名）。
1. 如果用户明确提到了景点/景区名称（如"兵马俑"、"故宫"）或文化主题（如"北欧文化"、"傣族泼水节"），请提取出来。可能有一个或多个，但不能重复。
2. 只有当当前问题使用代词（如"这里"、"它"、"当地"、"那里"）或明显省略了主体时，才结合历史会话做指代消解确定主体。
3. 【最高优先级】主体必须来自当前问题本身：严禁把历史会话中出现过、但当前问题未提及的主体带入 attractions 或 rewritten_query。当用户开启了新的行程/新的话题（当前问题已明确给出新地点）时，必须完全以当前问题为准，忽略历史中旧行程的目的地。
4. 【最高优先级例外——行程延续类追问，必须沿用历史出发地/目的地/日期】只要当前问题在延续、调整、细化上轮的行程规划（无论用问句、陈述句、省略句），就视为延续类追问。延续类典型句式（不限以下）：
   - 调整交通方式：「换成公共交通」「改坐高铁」「我想坐飞机去可以吗」「我想坐飞机去杭州」「坐飞机去」「我要坐飞机」「换飞机」「改飞机」「想坐高铁」「不想开车了」「不开车了」「开车太累」「自驾太累」「火车方便点」「改成大巴」「飞机快点」
   - 增减天数：「再安排一天」「多玩一天」「缩减一天」「玩 3 天就够」「改成两天一夜」
   - 行程细化：「明天先去西湖」「第三天改成灵隐寺」「酒店改五星」「不住西湖边了」
   - 行程质疑/微调：「行程太赶」「太满了」「轻松点」「预算砍半」「少花点钱」
   判定延续类的核心：当前问题的核心话题仍是历史中已确定的「出发地→目的地+日期」组合，只是其中某一项（交通/天数/单天安排/预算）在调整或质疑。
   遇到延续类追问时，必须结合历史会话补全出发地、目的地、原定日期，把问题改写为完整独立的规划问题（如「深圳到杭州飞机出行行程规划」），此时第 3 条的限制不适用于出发地/目的地。改写后的 rewritten_query 必须包含「出发地+目的地+交通方式或调整内容」完整信息（如「深圳到杭州飞机出行行程规划」），不得省略出发地。出发地/目的地只能取自历史会话或当前问题中明确出现的地名，严禁编造历史中不存在的地名（如历史中是深圳，就绝不能写北京/上海）。【本条优先级高于第 6 条】。
5. 如果无法确定主体名称，attractions 返回空列表（后续系统会进行全库检索兜底）。
6. 请重新改写用户的问题（rewritten_query），使其成为包含主体名称的独立完整问题，注意不要超过100个字符！非延续类追问场景下，改写时同样遵守第 3 条，不得引入当前问题未提及的地点或主体；延续类追问场景下，按第 4 条规则补全出发地/目的地/日期。

请直接返回JSON格式结果，格式如下：
{{
    "attractions": ["主体A", "主体B"],
    "rewritten_query": "关于主体A和主体B，..."
}}
