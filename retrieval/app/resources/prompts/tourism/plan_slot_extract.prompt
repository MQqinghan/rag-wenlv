历史会话：
{history_text}

当前用户问题：{query}

任务：判断用户在做旅行规划时，以下五项关键信息「是否已经明确给出」。只做"已给出 / 未给出"的判定，不要推测、不要脑补、不要替用户补全。

五项信息定义：
1. destination（目的地）：要去的城市 / 地区 / 景区（如"杭州""云南""九寨沟""川西小环线"）。只说"帮我规划一下""想去玩"而不给地点 → false。
2. origin（出发地）：从哪里出发（如"从成都出发""我在深圳"）。"我在 X""我到了 X""我到 X 了"等表示人已在当地的表述，同样算 true。
3. days（游玩天数）：行程时长（如"3 天""两天一夜""玩 5 天"）。
4. preference（旅行偏好）：个性化要求，含旅行偏好、同行人、节奏（如"喜欢自然风光""带孩子""不想太赶""轻松点"）。
5. budget（预算）：用户是否明确给出了总预算金额（如"预算5000""3000块搞定"→ true；只说"不想太贵""省钱点"等模糊表述 → false）。

判定规则：
- 只要当前问题【或】历史会话中明确出现了该项信息，即判 true。
- 历史会话中的信息仅在本轮明显是在延续同一次规划时才采信；若当前问题已开启一个全新的目的地 / 新行程，则历史里的旧信息不得算入本次。
- 含糊、需要猜测、只能靠常识默认的，一律判 false。例如"帮我规划一下杭州"→ 只有 destination 为 true。

请直接返回 JSON，不要输出任何解释文字：
{{
  "destination": true 或 false,
  "origin": true 或 false,
  "days": true 或 false,
  "preference": true 或 false,
  "budget": true 或 false
}}
