历史会话：
{history_text}

当前用户问题：{query}

你是检索查询扩展专家。为了提升知识库召回率，请一次性产出以下内容：

1. rewrites：2 个不同视角的改写查询，要求——
   - 保留原问题的核心实体（景点名 / 地名 / 文化主题名），不得臆造新实体
   - 两个改写之间要有明显差异：一个侧重"是什么 / 定义 / 概况"，一个侧重"怎么做 / 细节 / 对比 / 注意事项"
   - 每个改写不超过 60 字，独立成句，可直接用于检索

2. hyde_answer：一段 80-150 字的假设性答案，要求——
   - 直接写答案要点，不要写"根据…"、"综上所述"之类的套话
   - 围绕问题可能涉及的核心事实展开（地点、时间、票价、路线、成因、特点等）
   - 即使不确定也基于常识给出最可能的内容，不要输出"不知道"
   - 不要使用"假设"、"可能"等不确定的词汇

请直接返回JSON格式结果，格式如下：
{{
    "rewrites": ["改写查询1", "改写查询2"],
    "hyde_answer": "假设性答案正文…"
}}
